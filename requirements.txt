# Streamlit Community Cloud installs these for streamlit_app.py + pages/.
streamlit>=1.36
pandas>=2.0
numpy>=1.24
plotly>=5.20
pyarrow>=15.0
matplotlib>=3.7   # dashboard IC table uses Styler.background_gradient
scipy>=1.10       # pandas Spearman rank-IC (method="spearman") needs scipy
