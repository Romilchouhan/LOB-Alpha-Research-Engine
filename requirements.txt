# Streamlit Community Cloud installs these for streamlit_app.py + pages/.
streamlit>=1.36
pandas>=2.0
numpy>=1.24
plotly>=5.20
pyarrow>=15.0
matplotlib>=3.7   # dashboard IC table uses Styler.background_gradient
scipy>=1.10       # pandas Spearman rank-IC (method="spearman") needs scipy

# Phase 1 Workstream A — FI-2010 benchmark (python/fi2010/).
scikit-learn>=1.3  # LogisticRegression, StandardScaler, macro-F1
lightgbm>=4.0      # gradient-boosted multiclass classifier (needs libomp on macOS)
